// Generated by refactor from PHABizPush.xml

/// HIS2SA Data 层轻量结果规范。
/// 新 Data 方法建议返回 Ok/Fail 结构，旧方法可逐步迁移。
Class PHA.FACE.TPS.SA.HIS2SA.Data.DataResult Extends %RegisteredObject
{
ClassMethod Ok(data As %DynamicObject) As %DynamicObject
{
    Set result = {}
    Set result.success = 1
    Set result.message = "ok"
    Set result.data = $Select($IsObject(data):data,1:{})
    Quit result
}

ClassMethod Fail(message As %String) As %DynamicObject
{
    Set result = {}
    Set result.success = 0
    Set result.message = message
    Set result.data = {}
    Quit result
}

ClassMethod Ensure(result As %DynamicObject) As %DynamicObject
{
    If '$IsObject(result) {
        Quit ..Fail("业务数据为空或格式不合法")
    }
    If result.%IsDefined("success") && result.%IsDefined("data") {
        Quit result
    }
    Quit ..Ok(result)
}
}
